# VinuChain Testnet

VinuChain testnet (chain ID `206`) is the public test network where new
chain features activate before mainnet. This section covers connecting to
testnet, running a private fakenet, and the operator-facing chain upgrade
guide.

For all chain IDs, RPC endpoints, and explorer URLs in one place, see
[Network Details](../network-details.md).
